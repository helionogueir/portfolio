import type { BorderProps } from '../Border'
import type { MediaScreenProps } from '../MediaScreen'
import type { SizingProps } from '../Sizing'
import type { SpacingProps } from '../Spacing'
import type { WeightProps } from '../Weight'

export type BaseProps = {
  border: BorderProps
  mediaScreen: MediaScreenProps
  sizing: SizingProps
  spacing: SpacingProps
  weight: WeightProps
}
